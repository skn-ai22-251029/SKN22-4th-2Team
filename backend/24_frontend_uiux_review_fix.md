### 🛠️ 코드베이스 분석 결과
프론트엔드에서 IPC 카테고리 필터링 요청 시 백엔드의 `AnalyzeRequest` 모델에 `ipc_filters` 파라미터가 선언되어 있지 않아 데이터가 누락되는 크리티컬 버그를 수정했습니다. Pydantic 모델에 파라미터를 추가하고 이를 실제 VectorDB 조회(`search_multi_query`, `_execute_search`)까지 전달하도록 `PatentAgent`와 서비스 레이어의 호출부를 연동했습니다.

### 📋 PM 및 DevOps 전달용 백로그 (복사해서 각 에이전트에게 전달하세요)
- **Epic: RAG 로직 고도화 (Backend)**
  - [x] 프론트엔드 연동을 위한 `AnalyzeRequest` 모델 내 `ipc_filters` 파라미터 추가 완료
  - [x] `PatentAgent.analyze` 및 `search_with_grading` 메소드에 필터 조건 파싱 및 Pinecone 하이브리드 검색까지 파라미터 전달 파이프라인 구축 완료
- **Epic: API 엔드포인트 유지보수 (Frontend/Backend)**
  - [ ] 프론트엔드 측 `fetch /history` API 경로 Path -> Query Param 적용 작업 상태 확인 필요 (`fetch(\`${API_BASE_URL}/history?user_id=${USER_ID}\`)`)
  - [ ] History 사이드바 자동완성을 위한 응답 데이터의 `item.idea_text` -> `item.user_idea` 참조 수정 배포 확인
