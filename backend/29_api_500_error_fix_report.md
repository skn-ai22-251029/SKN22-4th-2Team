### 🛠️ 코드베이스 분석 결과 (API 500 에러 해결 완료)

*   **문제 원인 회고**:
    `src/api/main.py` 서버 구동 시, 필수 API 키가 주입되지 않은 상황(예: 로컬 환경에서 `.env` 부재 및 Secrets 흡수)에서도 앱 부트스트래핑 과정이 성공적으로 동작하여 uvicorn 서버가 정상 기동되었습니다. 그 결과, 애플리케이션 시작 시점이 아닌 **클라이언트가 `/api/v1/analyze` 엔드포인트를 호출하는 시점**에 뒤늦게 API 키 부재(`ValueError`) 오류가 발생하여 프론트엔드에 `HTTP 500 Internal Server Error`가 반환되는 문제가 있었습니다.
*   **개선 사항**:
    1.  `src/api/main.py`의 `bootstrap_secrets()` 실행 직후에 `config` 객체를 불러들여 `OPENAI_API_KEY`를 명시적으로 검증하도록 추가했습니다. 누락 시 즉시 예외를 발생시키고 `sys.exit(1)`로 앱을 강제 종료(Fast-fail)하도록 보안 및 안정성을 강화했습니다.
    2.  FastAPI의 `@asynccontextmanager(lifespan)`를 활용하여, 모듈 초기 로드 지연을 방지하고 트래픽을 수신하기 전에 `PatentAgent`와 `HistoryManager` 인스턴스를 미리 초기화(Pre-warm)하도록 구조를 리팩토링했습니다. 이로 인해 런타임 의존성 주입 시점의 병목 현상을 해소하고, 서버 시작 시점에 완전성을 미리 검증할 수 있게 되었습니다.

---

### 📋 PM 및 DevOps 전달용 백로그 (복사해서 각 에이전트에게 전달하세요)

- **Epic: RAG 로직 고도화 (Backend)**
  - [x] 필수 키(`OPENAI_API_KEY`) 검증 로직을 앱 시작(Bootstrapping) 시점으로 이동 (`src/api/main.py` 내 Fast-fail 처리 적용 완료)
  - [x] 의존성 사전 초기화 (Lifespan Context 적용): 트래픽 수신 전(Pre-warm) 단계에서 `PatentAgent` 인스턴스 등 핵심 의존성을 미리 초기화하여 앱 무결성 확보 완료

- **Epic: 컨테이너 및 인프라 구축 (DevOps에게 전달할 사항)**
  - [ ] **[주의] ALB/ECS 헬스 체크 정책 점검 요망**: Backend 수정으로 인해, 로컬 환경 또는 배포 환경에서 API Key 주입에 실패할 경우 컨테이너가 즉시 패닉 상태(`sys.exit(1)`)로 내려가도록 로직이 변경되었습니다. 따라서 환경변수가 미세팅된 경우 무한 재시도 루프(CrashLoopBackOff)가 발생할 수 있으므로 ECS Task Definition 주입 부분과 상태 점검(Target Group Health Check) 로직을 연계하여 확인해 주시기 바랍니다.
