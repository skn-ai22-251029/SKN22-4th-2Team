# [2026-02-27] ECS 배포 서비스 MISSING 에러 해결 가이드 및 초기 인프라 세팅

## 주요 문제 사항
방금 배포 파이프라인의 마지막 단계인 `ECS 서비스에 새 Task Definition 배포` 과정에서 아래와 같은 에러가 발생했습니다:
1. **에러 내용**: `arn:aws:ecs:***:***:service/*** is MISSING`
2. **원인 분석**:
   - `aws-actions/amazon-ecs-deploy-task-definition` 액션은 GitHub Secrets(`ECS_CLUSTER_PROD`, `ECS_SERVICE_PROD`)에 저장된 이름값을 가지고 AWS를 찌릅니다.
   - 하지만 **AWS 상에 해당 이름의 ECS 클러스터(Cluster)나 서비스(Service)가 아직 생성되어 있지 않기 때문에**, 도달해야 할 대상 리소스를 찾지 못해(MISSING) 발생한 에러입니다.
   - 아직 '쇼특허'의 프로덕션용 초기 AWS 인프라(네트워크, 클러스터, 서비스 자원)가 프로비저닝되지 않은 상태로 파악됩니다.

## 해결 방법
파이프라인이 정상적으로 돌아가려면 도착지가 될 **실제 AWS 인프라 자원**이 먼저 구성되어야 합니다. 또한, 그 이름이 GitHub Secrets와 일치해야 합니다.

### 인프라 셋업 및 문제 해결 절차

1. **AWS 콘솔에서 ECS 클러스터 및 서비스 프로비저닝**
   - **ECS(Elastic Container Service)** 메뉴로 들어갑니다.
   - 운영 환경용 **클러스터**를 먼저 생성합니다. (예: `short-cut-prod-cluster`)
   - 해당 클러스터 내부에 **서비스(Service)**를 생성합니다. 
     - 컴퓨팅 옵션: 가성비가 좋고 관리가 편한 **Fargate** 권장
     - 패밀리(Task Definition): 앞서 성공한 파이프라인 단계에서 이미 `short-cut-api` 이름으로 초기 Task Definition이 만들어져 있을 것입니다. 이를 선택합니다.
     - 네트워킹 및 로드밸런서(ALB) 구성 시 8000 포트로 맞춥니다.
     - 서비스 이름 지정: (예: `short-cut-api-service`)

2. **GitHub Secrets 동기화**
   - GitHub **Settings > Secrets and variables > Actions** 로 이동합니다.
   - 내가 방금 AWS 콘솔에서 만든 실제 이름을 입력해 줍니다.
     - `ECS_CLUSTER_PROD` : 예) `short-cut-prod-cluster`
     - `ECS_SERVICE_PROD` : 예) `short-cut-api-service`

이렇게 초기 인프라라는 그릇을 AWS에 먼저 만들어 주시고, 이름을 일치시킨 뒤 재배포를 해주시면, 앞으로는 코드 수정 뒤 PUSH만 해도 자동으로 서비스가 교체되는 완벽한 CI/CD 라이프사이클을 누리실 수 있습니다!

---

### 📋 PM 에이전트 전달용 기술 백로그 (복사해서 PM에게 전달하세요)
- **Epic: AWS 인프라 프로비저닝 (초기 세팅)**
  - [ ] AWS 콘솔에서 ECS 운영 클러스터(Cluster) 생성 
  - [ ] 해당 클러스터 내에 AWS Fargate 기반 ECS 서비스(Service) 신규 생성 (태스크로 `short-cut-api` 연결)
  - [ ] GitHub > Actions Secrets에 생성한 `ECS_CLUSTER_PROD` 및 `ECS_SERVICE_PROD` 값 정확하게 입력/업데이트
